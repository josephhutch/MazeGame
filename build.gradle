apply plugin: 'java'
jar {
    manifest {
        attributes( 'Main-Class': 'engine.Core')
    }
}
sourceSets {
    main {
        java {
            srcDirs = ['engine', 'io', 'aStar', 'recursiveBacktracker']
        }
    }
}
